# Alertmanager notification secrets
#
# Copy this file to alertmanager.env and fill in real values.
# Then source it before running docker-compose:
#   source alertmanager.env && docker compose up -d
#
# IMPORTANT: Never commit real secrets to git!

# Slack incoming webhook URL
# Create at: https://api.slack.com/messaging/webhooks
# Destination channel: #nullspace-alerts (warnings) and #nullspace-critical (critical)
SLACK_WEBHOOK_URL=your-slack-webhook-url-here

# PagerDuty Events API v2 integration key
# Create at: PagerDuty > Services > [Your Service] > Integrations > Events API V2
# Used for critical alerts that page on-call
PAGERDUTY_SERVICE_KEY=0000000000000000000000000000000000

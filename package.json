{
  "name": "nullspace",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "test:all": "pnpm test && cargo test --workspace && (cd evm && npm test)",
    "coverage:all": "node scripts/coverage-all.mjs",
    "load-test": "./scripts/load-test.sh",
    "load-test:quick": "./scripts/load-test.sh --quick",
    "load-test:gateway": "./scripts/load-test.sh --gateway-only",
    "load-test:engine": "./scripts/load-test.sh --engine-only",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
